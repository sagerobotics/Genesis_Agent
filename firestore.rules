rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 👇 TEMPORARY: open for testing & to test if this works Alex McClarty
      allow read, write: if true;
    }
  }
}
